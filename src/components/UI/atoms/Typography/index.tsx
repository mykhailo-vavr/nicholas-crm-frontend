import { Typography as MUITypography } from '@mui/material';
import { TypographyFC } from './types';

const Typography: TypographyFC = (props) => <MUITypography {...props} />;

export default Typography;
